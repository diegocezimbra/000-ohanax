version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No build needed for static site"
    build:
      commands:
        - echo "Build complete"
  artifacts:
    baseDirectory: landing
    files:
      - '**/*'
